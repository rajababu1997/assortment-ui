/**
 * Shared visual constants for Sales History card chrome.
 *
 * Keeping the header background in one place means a future palette
 * tweak is one line, not twelve. Same gradient as the page-title icon
 * chip — soft blue→violet, almost imperceptible at rest but enough to
 * separate the header strip from the body content below it.
 */

export const HEADER_BG =
  'linear-gradient(90deg, rgba(96,165,250,0.10), rgba(167,139,250,0.05))';
