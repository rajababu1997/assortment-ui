/**
 * Shared form message constants — ported from Angular:
 *   core/constants/enum-constants.ts → FORM_REQUIRED_MESSAGE
 *   shared/constants/tps-enum-constants.ts → FORM_REQUIRED_MESSAGE
 *
 * Import and render at the top of every form that has mandatory fields.
 */

export const FORM_REQUIRED_MESSAGE = 'Note: Fields marked with (*) are mandatory';
